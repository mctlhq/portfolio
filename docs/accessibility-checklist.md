# Accessibility checklist (WCAG 2.2 AA)

One row per item named in issue #10 (P8). `pass` and `fail` are backed by a
committed file, a build-time script, or a direct read of the source named in
the note; `reviewer step` is work that genuinely needs a human (a screen
reader, a Lighthouse run, a browser) and is never treated as an acceptance
criterion — see AGENTS.md, "Issue contract".

| Item | Result | Note |
| --- | --- | --- |
| Keyboard operation of every `<details>` | pass | `src/components/Details.astro` renders a native `<details>`/`<summary>` with no JavaScript interception of focus, `Enter`/`Space`, or the arrow keys — the browser's built-in disclosure behaviour handles all of it. `src/styles/site.css` sets `.block > summary { display: list-item }` so the native marker (and therefore the native keyboard affordance) stays intact rather than being replaced by a custom control. |
| Focus visibility on links and buttons | pass | `src/styles/site.css` keeps a global `:focus-visible { outline: var(--focus-ring-width) solid var(--focus-ring); outline-offset: var(--focus-ring-offset); }` rule with no element carrying `outline: none`; asserted mechanically by `test/a11y.test.ts`. The ring colour is `--focus-ring: var(--accent)`, whose contrast against both surfaces is checked below. |
| Contrast in both themes | pass | `scripts/check-contrast.mjs` (wired into `npm test`) computes the WCAG relative-luminance contrast ratio for every foreground/background pair `site.css` renders — `surface-fg`, `surface-fg-muted` and `accent` over `surface-bg` and `surface-elevated`, plus `accent-fg` over `accent` — for both `data-theme` values, plus the content-link states below. All 26 pairs (the number `check-contrast.mjs` prints today) clear their threshold with margin: the tightest is `accent` (the focus ring colour) over `surface-bg` in light theme at 4.81:1 against a 3:1 minimum; every text pair is at or above 4.81:1 against a 4.5:1 minimum. |
| Content link contrast | pass | Issue #46: every unstyled `<a>` inside `<main>` used to render at the browser default blue/purple, which fails 4.5:1 on the dark surface. `src/styles/site.css` now declares `main a`, `main a:visited` (`color: var(--accent)`) and `main a:hover` (`color: var(--accent-highlight)`); `.cta:visited` and `.project-links a:visited` are pinned back to `var(--surface-fg)` so the higher `:visited` specificity does not repaint existing CTA/work links. `main a` was chosen over adding `class="mctl-prose"` to `<main>` because `.mctl-prose a` would resolve to specificity (0,1,1), beating `.cta` at (0,1,0) and restyling every CTA button, and because `Base.astro` has no `<main>` element of its own to carry the class (each page owns its `<main>`). `scripts/check-contrast.mjs`'s `linkColourProblems()` (asserted by `test/contrast-link.test.ts`) measures: dark 5.40:1 (`main a`/`main a:visited` over `surface-bg`) / 5.19:1 (over `surface-elevated`); light 4.81:1 / 5.11:1; hover dark 8.53:1 / 8.19:1; hover light 6.30:1 / 6.70:1. `test/a11y.test.ts` asserts the rules exist at the source level. |
| `lang` correct on RU blocks | pass | `src/i18n/Lang.astro` renders `<span class="l ru" lang="ru">`; every Russian block on the site goes through this component or repeats the same `class="l ru" lang="ru"` pattern directly (the `<ul class="l ru" lang="ru">` blocks on the home and approach pages). Asserted by `test/a11y.test.ts` against `Lang.astro`, and by the existing `class="l en"` / `class="l ru"` parity check in `scripts/check-dist.mjs`. |
| Heading hierarchy | pass | Every page has exactly one `<h1>` (`src/pages/index.astro`, `work.astro`, `approach.astro`, `colophon/index.astro`, `404.astro`, and the two dynamic colophon routes), with `<h2>` used for page sections (`work.astro`'s two groups, `approach.astro`'s `<Details>` summaries are `<summary>` not headings, `colophon/index.astro`'s three sections, the journal and ADR pages' section headings). No page skips a level or uses a heading for styling alone. |
| Link purpose | pass | Every link's accessible text comes from `src/i18n/ui.ts` (`ctaWork`, `ctaColophon`, `navHome`/`navWork`/`navApproach`/`navColophon`, `journalBackToColophon`, `adrBackToColophon`, `notFoundHome`, …) or names its destination directly (`github.com/mctlhq`, `hello@dmitriimashkov.com`, a project's own name in `ProjectCard.astro`, an ADR's own title). None reads "click here" or "read more". |
| SVG text alternatives | pass | The two `<CycleDiagram>` variants (`src/components/CycleDiagram.astro`) each carry `role="img"`, `aria-labelledby` resolving to one `<title>` and one `<desc>` inside the same `<svg>` — enforced mechanically by `checkApproachPage()` in `scripts/check-dist.mjs`, which fails the build on a missing role, a dangling `aria-labelledby` token, or a wrong `<title>`/`<desc>` count. `public/og.svg` (never fetched by the page itself; only referenced as `og:image`/`twitter:image`) also carries `role="img"` and a `<title>`/`<desc>` pair, though it is not covered by the build check since it is a static `public/` asset the build never inspects. |
| Target size at least 24px | pass | `src/styles/site.css` sets `min-block-size: 44px` — above the 24px CSS Working Group "AA equivalent" floor named in the issue — on `.site-nav a`, `.toggle-group button`, `.site-footer a`, `.cta` and `.block > summary`; asserted by `test/a11y.test.ts`. |
| No motion | pass | `src/styles/site.css` declares no `animation` or `transition` property anywhere in the file (grep-verified and asserted by `test/a11y.test.ts`); the only visual change on interaction is the `[aria-pressed='true']` colour swap on the language/theme toggles and the `:focus-visible` outline, both instantaneous. |
| Screen-reader sweep (NVDA/VoiceOver) over all four pages, both languages | reviewer step | Needs an actual screen reader; not reproducible from source or from a build script. |
| Visual focus confirmation (tab through every page, both themes) | reviewer step | Needs a real browser viewport to confirm the ring is visually legible in context, beyond the numeric contrast ratio `check-contrast.mjs` already proves. |
| Lighthouse re-run of `/colophon/` (issue #46) | reviewer step | The new content-link colours and the footer/colophon link changes touch `/colophon/` specifically; a Lighthouse accessibility pass in a real browser is not reproducible from source or from a build script and is not treated as an acceptance criterion — see AGENTS.md, "Issue contract". |

No item above is marked `fail`, so there is no documented exception or
follow-up issue to open at this time.
